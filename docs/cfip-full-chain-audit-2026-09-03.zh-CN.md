# CFIP 优选 IP 全链路审计记录（2026-09-03）

## 结论

本次取得了新的自然周期样本，CFIP 生产链路正常运行，未发现需要修改代码或生产配置的可复现 bug。没有执行手动测速、补跑同步、Cloudflare 写入、PassWall 重启、DNS 修改或路由修改。

## `.110` Sidecar

- 自然 Sidecar 结果为 `success`，退出码为 `0`，`MainPID=0`。
- timer 为 `active/enabled`，下一次自然运行已排程。
- 最新报告包含 5 行，均为 HTTP 200；最新导出包含 3 个 `observation` 候选。
- 候选代理路径速度约为 `5.20–5.23 MB/s`，低于 `6.5 MB/s` 优秀候选标志，但高于 `3.5 MB/s` 观察底线；保持现有门控是正确行为。
- Sidecar 锁空闲，无临时 Xray 配置、瞬时 CFIP 容器或 `cfip-direct` 附着；`sub2api`、PostgreSQL、Redis 均为 running/healthy。
- Ollama 当时有 1 个驻留模型。该状态未导致 Sidecar 失败，也没有证据表明它占满资源，因此本次不强制卸载或重启。

## VM36 自动同步与候选池

- 04:35 自动同步本次状态为 `already_present`，表示当前五个 Cloudflare 槽位已经符合本周期排序目标，没有执行写入。
- 主槽资格池有 3 个连续通过候选；竞争资格池有 2 个连续通过候选，均来自真实隔离 Xray/PassWall 路径。
- `auto`、`auto1`、`auto2` 当前为 3 个不同 IP；`auto3`、`auto4` 也没有与主槽重复。
- 04:35 同步 cron 唯一存在，旧 06:30 cron 不存在；四把项目锁空闲，无 CFIP 进程残留。

## PassWall、DNS 与 Cloudflare

- VM36 只有 1 个有效 Xray 进程，`1070/1041/11400/15353` 四个监听均正常。
- VM36 Google/YouTube HTTP 204 通过。
- `auto` 至 `auto4` 在 `192.168.1.1`、`192.168.1.254` 和 `1.1.1.1` 三路解析逐条一致。
- Cloudflare 五条 A 记录只读 GET 全部 HTTP 200，内容与三路 DNS 一致。

## 未修复的独立异常

本次 `.110` 宿主直连 Google 和 YouTube 仍返回 HTTP 000/连接超时。这是宿主直连出口路径异常，不等同于 CFIP 生产链路故障：Sidecar 通过其代理路径完成自然测速并导出，VM36 生产代理和四监听正常。没有证据证明修改 DNS、PassWall 或路由可以安全解决该问题，因此本次不改动这些生产组件。

此前 `.140` 维护跳板曾出现一次 SSH 握手超时，随后恢复可达；本次审计最终通过既有严格主机密钥和受保护维护通道完成，未绕过凭据边界。

## 本地验证

- 生产脚本 `bash -n` 通过。
- 全项目回归测试通过。
- Sidecar 全套测试通过。
- 候选导出、安装幂等、诊断契约、路径重试、Xray 残留保护和宿主清理策略测试通过。
- Windows Git Bash 中依赖真实 Linux `flock` 的运行时测试按环境限制跳过；这不是生产 Linux 锁测试失败。

## 收口建议

当前项目保持现状运行，不降低门槛，不增加 DNS，不恢复旧 06:30 任务，不手工改写 `auto` 至 `auto4`，也不继续扩大架构。后续按自然周期观察即可；只有出现新的自然周期失败、五槽重复或三路 DNS/Cloudflare 内容不一致时，才启动下一轮最小修复。
