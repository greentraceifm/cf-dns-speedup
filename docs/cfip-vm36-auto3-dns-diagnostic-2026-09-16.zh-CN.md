# VM36 auto3 本地 DNS 空答案只读诊断（2026-09-16）

## 结论

`auto3.greentraceifm.top` 在 VM36 DNS `192.168.1.254` 上的空答案未复现。2026-09-16 17:00 CST 从 `.140` 连续执行三轮新查询，VM36、上级路由器 `192.168.1.1` 和公共 DNS `1.1.1.1` 均返回 `NOERROR`、一个 A 记录和相同地址 `104.26.1.38`，没有超时、`SERVFAIL` 或 `NXDOMAIN`。

其他 `auto`、`auto1`、`auto2`、`auto4` 也在三条 DNS 路径返回一致地址。结合此前 Cloudflare 只读 GET 正常，当前证据支持：**先前空答案是一次性查询或瞬时转发异常，不是权威 DNS 损坏，也没有证据证明 VM36 存在持续缓存或监听故障。按既定规则记录并停止，不执行修复。**

## 固定维护链路

- 本机可以严格主机密钥校验连接 `192.168.1.140`。
- `.140` 主机名为 `openclaw-server`。
- `.140 -> .110` 固定只读入口正常。
- `.140 -> VM36` 的 SSH 密码和现有密钥认证仍未恢复，因此未读取 VM36 内部配置和日志；DNS 查询直接从 `.140` 发往 VM36 的 53 端口完成，不需要绕过维护边界。

## `.110` 旁证

通过 `.140` 固定入口取得的新鲜只读审计结果（远端时间：2026-09-16 06:37:24 UTC）：

- Sidecar 最近任务结果：`success`
- Sidecar timer：`active`、`enabled`
- 报告 5 行，导出 3 行
- Xray 残留、瞬时 CFIP 容器、`cfip-direct` 附着：均为 0
- `sub2api`、PostgreSQL、Redis：`running/healthy`
- Ollama 驻留模型：0

`.110` 直连 Google 和 YouTube 为 HTTP 000 超时，属于既知宿主直连出口现象，不作为 CFIP 或 VM36 DNS 故障证据。

## auto3 连续三轮查询

查询时间：2026-09-16 17:00:55 CST。查询均使用 UDP/53，超时上限 3 秒、单次尝试。

| 轮次 | DNS 服务器 | 状态 | 答案 | TTL | 查询时间 |
|---|---|---|---|---:|---:|
| 1 | `192.168.1.1` | `NOERROR` | `104.26.1.38` | 60 | 211 ms |
| 1 | `192.168.1.254` | `NOERROR` | `104.26.1.38` | 1 | 75 ms |
| 1 | `1.1.1.1` | `NOERROR` | `104.26.1.38` | 1 | 0 ms |
| 2 | `192.168.1.1` | `NOERROR` | `104.26.1.38` | 59 | 0 ms |
| 2 | `192.168.1.254` | `NOERROR` | `104.26.1.38` | 60 | 7 ms |
| 2 | `1.1.1.1` | `NOERROR` | `104.26.1.38` | 60 | 0 ms |
| 3 | `192.168.1.1` | `NOERROR` | `104.26.1.38` | 58 | 0 ms |
| 3 | `192.168.1.254` | `NOERROR` | `104.26.1.38` | 59 | 0 ms |
| 3 | `1.1.1.1` | `NOERROR` | `104.26.1.38` | 59 | 0 ms |

第一轮 TTL 为 1，随后刷新为 60 并正常递减，符合短 TTL 记录在缓存临界点重新获取后的表现，不构成负缓存或错误缓存证据。

## 其他槽位查询

| 记录 | 当前地址 | `192.168.1.1` | `192.168.1.254` | `1.1.1.1` |
|---|---|---|---|---|
| `auto` | `104.26.1.38` | `NOERROR` | `NOERROR` | `NOERROR` |
| `auto1` | `104.17.153.15` | `NOERROR` | `NOERROR` | `NOERROR` |
| `auto2` | `104.17.129.81` | `NOERROR` | `NOERROR` | `NOERROR` |
| `auto4` | `104.17.158.61` | `NOERROR` | `NOERROR` | `NOERROR` |

所有路径均返回一个 A 记录，没有空答案、超时、`SERVFAIL` 或 `NXDOMAIN`。

## 分类判断

| 分类 | 判断 | 依据 |
|---|---|---|
| 权威 DNS 问题 | 排除 | Cloudflare 既有只读 GET 正常；当前 `1.1.1.1` 三轮均返回正确地址 |
| VM36 持续缓存问题 | 未发现 | `.254` 三轮均正确；TTL 从临界值刷新为 60 后正常递减 |
| VM36 转发或监听问题 | 当前排除 | `.254:53` 连续响应，五个槽位均有一致答案 |
| 一次性查询异常 | 最符合证据 | 原空答案未复现，当前连续多轮全部正常 |
| VM36 SSH 维护认证 | 独立待修复 | SSH 服务提供 `publickey,password`，但现有受保护凭据和密钥均未认证成功 |

## 修复建议

本次不执行、也不建议执行 DNS 修复。无需清缓存、重载 dnsmasq、重启 PassWall、增加 DNS 上游或修改 Cloudflare。

只有未来再次出现“仅 `.254` 空答案、`.1` 与 `1.1.1.1` 正常”的可复现样本时，才应先保存连续查询和相邻时间窗口日志，再单独评估最小缓存重载。本轮没有确认缓存故障，因此不提供或执行重载命令。

VM36 SSH 维护认证应作为独立维护问题处理，不与本次已收口的 DNS 一次性异常混合修复。

## 安全边界

本轮未修改 VM36、DNS、Cloudflare、PassWall、Sidecar、Docker、Ollama、cron、timer、路由、防火墙或 VM；未清缓存、重载、重启或手动补跑任何生产任务。

用户提供的维护凭据仅写入 `.140` 权限为 `0600` 的既有受保护配置并用于 SSH 验证，未输出到报告或日志；VM36 拒绝该凭据。该认证问题不影响本次从 `.140` 对 VM36 DNS 服务完成的只读诊断。
